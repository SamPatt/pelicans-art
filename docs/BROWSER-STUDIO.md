# Editor

The public visual workflow is now the [companion Editor](EDITOR.md): open the project your agent made, make visual or dialogue edits, preview, and export it back to your agent.

The previous Studio interface is retained at `legacy-studio.html` for migration and rollback. It is not the default creation path. Existing legacy browser data is unchanged; export a built project from the old interface, then open it in Editor.
